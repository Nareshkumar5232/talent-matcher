# Firebase Configuration
# =======================
# You need to configure Firebase credentials to connect to Firestore.
# There are two ways to do this:

# Option 1: Service Account JSON file (Recommended for local development)
# -------------------------------------------------------------------------
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate new private key"
# 3. Save the file as `serviceAccountKey.json` in the `backend/config/` folder
# 4. Make sure to add this file to .gitignore!

# Option 2: Environment Variables (Recommended for production/Vercel)
# ---------------------------------------------------------------------
# Set these environment variables:

# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# OR use the full service account JSON as a single variable:
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"..."}

# Server Configuration
PORT=5000
